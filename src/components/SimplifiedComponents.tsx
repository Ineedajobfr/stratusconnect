// Simplified component exports to avoid import issues
import React from 'react';
import { Brain, CheckCircle } from 'lucide-react';
import { Button } from '@/components/ui/button';

// Export missing icons and components
export { Brain, CheckCircle, Button };

// Simple voice settings
export const voiceSettings = {
  voice: 'default',
  rate: 1.0,
  pitch: 1.0,
  volume: 1.0
};